package ai.shreds.domain.entities;

/**
 * Enumeration of supported protocol types for GPS data and device protocols.
 */
public enum SharedProtocolTypeEnum {
    UNKNOWN,
    NMEA,
    GPX,
    UBLOX
}